function circleArea(radius) {
  if (radius <= 0) {
 throw new Error('Radius must be positive');
    return;
  } else {
    let area = Math.PI * radius * radius;
    console.log(area);
    return area;
  }
}

circleArea(13); 